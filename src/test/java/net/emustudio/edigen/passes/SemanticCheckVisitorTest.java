package net.emustudio.edigen.passes;

public class SemanticCheckVisitorTest {

    // subrule in the middle of rule cannot be without length
    //  (only if their definition has length defined)

    // subrule at the end (last one) in the rule can have no length
    // (why? how is it inferred?)

    // values in rules must be "returning" - return a value

    // set of values in any two disassembler formats should not be the same


}
